# راهنمای پارسی‌نگار

این راهنما کار با میزکار پارسی‌نگار را قدم‌به‌قدم توضیح می‌دهد.

## نمای کلی

میزکار پنج بخش دارد: نوار منو (بالا)، ریل فعالیت (کنار)، پنل کناری (فایل‌ها، فهرست مطالب، تنظیمات)، ادیتور (وسط) و نوار وضعیت (پایین). همه‌چیز راست‌به‌چپ و فارسی است و اسناد به‌صورت خودکار روی همین دستگاه ذخیره می‌شوند.

## اسناد

- **سند تازه** و **بارگذاری سند** (فایل `.md`) از منوی پرونده یا دکمه‌های بالای فهرست فایل‌ها.
- هر فایل منوی خودش را دارد: **تغییر نام** (درون‌خطی)، **دانلود مارک‌داون**، **ویژگی‌های پرونده** و **حذف** با تأیید.
- **چینش فایل‌ها** از دکمه بالای فهرست: نام (الفبایی و عکس)، زمان ویرایش و زمان ساخت.
- ویرایش‌ها پس از کمی مکث خودکار ذخیره می‌شوند؛ نیازی به دکمه ذخیره نیست.

## ویرایش متن

نشان‌های مارک‌داون را یا مستقیم تایپ کنید یا از منوی **افزودن** و میان‌برها استفاده کنید:

- عنوان `Ctrl+H`، پررنگ `Ctrl+B`، مورب `Ctrl+I`، خط‌خورده `Ctrl+Shift+S`
- نقل‌قول `Ctrl+Q`، پیوند `Ctrl+K`، کد `Ctrl+E`
- فهرست نامرتب `Ctrl+Shift+U` و مرتب `Ctrl+Shift+L`
- `Tab` تورفتگی و `Shift+Tab` بیرون‌بردگی؛ `Alt+Arrow` جابه‌جایی خط
- `Enter` روی بازبینه و فهرست، آیتم بعدی را می‌سازد (روی آیتم خالی خارج می‌شود)
- جفت‌کاراکترها (`()` و `[]` و `{}` و «» و بک‌تیک) خودکار بسته می‌شوند

بازبینه تسک (`- [ ]`) با کلیک روی مربع تیک می‌خورد. بلوک‌های کد هایلایت می‌شوند و دکمه رونوشت دارند. تصاویر راه‌دور (`![alt](https://…)`) مستقیم دیده می‌شوند؛ روی تصویر بایستید تا سورس آن برای ویرایش برگردد.

## جست‌وجو

با `Ctrl+F` (یا ذره‌بین ته منوبار) دراپ‌دان جست‌وجو باز می‌شود: عبارت، جایگزینی، حساسیت به حروف، کلمه کامل، عبارت باقاعده و «فقط در انتخاب». `Enter` به مورد بعدی می‌رود و شمارنده موقعیت را نشان می‌دهد. `Ctrl+Shift+F` مستقیم روی فیلد جایگزینی متمرکز می‌شود. جست‌وجو فقط همین سند را می‌گردد.

## حالت فقط-خواندن

- **قفل سند** از منوی پرونده، دکمه قفل کنار ذره‌بین یا جفت «حالت خواندن»/«حالت نوشتن» در منوی نمایش: سند قفل می‌شود (نشان «فقط خواندنی» در نوار وضعیت) و تایپ و تغییر ناممکن می‌شود، ولی انتخاب، رونوشت و جست‌وجو آزاد است.
- اسناد **راهنما**، **تاریخچه تغییرات** و **درباره** از منوی «راهنما» همیشه فقط-خواندن‌اند و ربطی به اسناد شما ندارند.

## نماها و تنظیمات

- ریل فعالیت بین سه نما جابه‌جا می‌شود: **فایل‌ها**، **فهرست مطالب** (کلیک روی عنوان، کرسر را می‌برد و با اسکرول همگام است) و **تنظیمات**.
- در تنظیمات: پوسته (روشن، تیره، دستگاه، سپیا)، جهت متن (خودکار، راست‌به‌چپ، چپ‌به‌راست) و اندازه قلم — همه پایدار می‌مانند.
- نوار وضعیت پایین، آمار زنده سند (نویسه، حرف، واژه، خط، حجم) را با ارقام فارسی نشان می‌دهد.
