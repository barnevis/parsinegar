# تاریخچهٔ تغییرات پارسی‌نگار

تمام تغییرات قابل‌توجه این پروژه در این فایل ثبت می‌شود.

## [Unreleased]

### افزوده‌شده
- بخش تنظیمات: آیکون چرخ‌دنده در انتهای ریل با نمای تنظیمات در پنل کناری — پوسته (روشن/تیره/دستگاه/سپیا با پالت کامل)، جهت متن سند (خودکار/راست‌به‌چپ/چپ‌به‌راست) و استپر اندازه قلم ویرایشگر (۱۲ تا ۲۴ پیکسل).
- افزونهٔ `parsinegar.settings`: ترجیحات اعتبارشده با پیش‌فرض روی `pey.storage` (کالکشن `settings`)، رویداد `settings:changed` و گزارش خطای بحرانی با از دست رفتن استورج.
- پل تم در Entry: استور shared-state کیت + بازتاب روی `documentElement` تا استایل‌های سطح سند و ادیتور هم ببینند؛ `public/theme.css` با اسکوپ‌های تیره و مدیاکوئری سیستم.
- زنجیره‌سازی نوشتن‌های پشت‌سرهم تنظیمات در home و Entry (همگرایی ترتیبی بدون race).
- مهاجرت اسکیمای استورج به نسخه ۲ (ساخت کالکشن `settings` روی پروفایل‌های قدیمی با حفظ سندها).
- پین شدن آیکون تنظیمات به انتهای ریل فعالیت (`align: 'end'` در رجیستری نماها).
- آمار حرف (فقط حروف یونیکد) و حجم پرونده (بایت/کیلوبایت) در نوار وضعیت.
- اسکرول‌اسپای فهرست مطالب: برجسته شدن عنوان فعال هنگام پیمایش ویرایشگر.
- منوی «افزودن» با ۹ نشان مارک‌داون (عنوان، پررنگ، مورب، خط‌خورده، نقل‌قول، پیوند، کد، فهرست مرتب/نامرتب) که در متن درج می‌شوند.
- قلم ۱۲ پیکسل نوار منو و حذف پس‌زمینه دکمه بازشونده.
- لهجه teal پیشین (`#5eead4`) در تم تیره و هایلایت `==` در هر سه پوسته (زرد/لیمویی/کهربایی).
- سطح کروم نوار منو و ریل فعالیت (پس‌زمینه + جداکننده هم‌ارز پنل و نوار وضعیت) در تم‌های تیره و سپیا.
- حذف فاصله بین نوار منو و پنل/ریل (حذف `sticky/top` قدیمی اسلات‌ها که به ارتفاع منوبار گره خورده بود).
- نگاشت `device` به `system` کیت هنگام اعمال تم (بدون آن، صفحه روشن می‌ماند ولی ادیتور در سیستم تیره، متن روشن روی زمینه روشن می‌شد).
- ترکیب فرزند واقعی میزکار: چهار المنت (`parsi-menu-bar`، `parsi-activity-rail`، `parsi-side-panel`، `parsi-status-bar`) که صفحهٔ خانه از طریق هلپر مشترک `src/ui/utils/mount.js` مونت می‌کند؛ ارتباط برگشتی فقط رویداد حبابی DOM است.
- ثبت استثنای ترکیب فرزند در `docs/decisions.md` §۱۱ (هلپر `mount.js` تا وقتی کیت ترکیب خودش را بدهد).

### حذف‌شده
- رندر رشته‌ای ناحیه‌ها (`regions.js` و تستش)؛ استایل و منطق هر ناحیه به المنت خودش رفت.

### تغییرکرده
- چیدمان پوشه‌ها طبق `file-layout` بنیان: هر فرزند میزکار در پوشه خودش (`components/<name>/` با `.js` و `.md` هم‌نام)؛ تست‌ها در آینه `tests/components/<name>/`؛ هلپرهای مشترک در `components/workbench/` ماندند.

## [0.4.0] - 1405-06-21

### افزوده‌شده
- تشخیص خودکار جهت هر خط ادیتور: خط فارسی راست‌چین و خط انگلیسی چپ‌چین می‌شود (`unicode-bidi: plaintext` روی هر خط).
- تأییدیه حذف سند با نمایش نام سند در مدال وسط صفحه، هم در پنل فایل‌ها و هم در منوی پرونده.
- کلیدهای میان‌بر ویرایشگر (پررنگ، کج، خط‌خورده، عنوان، نقل‌قول، پیوند، کد و فهرست‌های مرتب و نامرتب) که با موقعیت فیزیکی کلید کار می‌کنند و به چیدمان فارسی/انگلیسی وابسته نیستند.

## [0.3.0] - 1405-06-20

### افزوده‌شده
- مدیریت چندسند با `pey.storage` (IndexedDB) و افزونهٔ `parsinegar.documents`: فهرست/بازکردن/ساخت/حذف و autosave یک‌ثانیه‌ای.
- میزکار: نوار منوی دراپ‌داون (پرونده/ویرایش/نمایش)، ریل آیکون راست با پنل کناری قابل‌تعویض (فایل‌ها/فهرست مطالب)، کارت ویرایشگر و نوار وضعیت زنده (نویسه/واژه/خط).

### تغییرکرده

- ساختار افزونه‌ها آینهٔ افزونه‌های Pey شد: `docs/reference.md`، `lib/` (برای منطق جدا از سیم‌کشی) و `CHANGELOG.md` برای هر افزونه.
- آزمون‌ها هم‌مکان شدند: افزونه‌ها در `tests/` خودشان، رابط کاربری در `src/ui/tests/` (مثل کیت)، و ریشه فقط برای سطح اپ و یکپارچه‌سازی.

### رفع‌شده

- خطاهای رهاشده در جریان‌های سند (تعویض/ساخت/حذف) با `try/catch` مهار شدند.
- شناسهٔ سند بدون `crypto.randomUUID` (HTTP غیرامن) هم ساخته می‌شود.
- جهت ادیتور از پیکربندی می‌آید؛ اعداد آمار فارسی‌اند؛ سطوح از توکن‌های `--pey-*` پیروی می‌کنند؛ چیدمان در عرض کم تاشو است.
- استایل صفحه به شیت خارجی واقعی (`home.css` + `stylesheetHref` کیت) مهاجرت کرد؛ مونت ادیتور با مشاهده‌گر render انجام می‌شود تا پشت گیت استایل گیر نکند.


## [0.2.0] - 1405-06-19

### افزوده‌شده

- نمای زندهٔ تک‌صفحه‌ای ویرایشگر: پنهان‌سازی نشان‌های Markdown، رندر عنوان‌ها/نقل‌قول/فهرست/لینک/کد، کارت کاغذی صفحه و کلیک-برای-فوکوس.
- قالب‌های بازبینه (`- [ ]`/`- [x]` با چک‌باکس کلیک‌شونده)، خط‌خورده (`~~`) و برجسته (`== ==`) با مبنای GFM.

### تغییرکرده

- بستار import استاتیک CodeMirror (`@codemirror/*` ،`@lezer/*` و وابسته‌ها) از نصب transitive به وابستگی مستقیم `package.json` ارتقا یافت؛ همه در importmap نگاشت شده‌اند.

### رفع‌شده

- انتخاب کل متن با Ctrl/⌘+A در چیدمان فارسی (بر اساس موقعیت فیزیکی کلید، نه `event.key`).


## [0.1.0] — 1405-06-16

### افزوده‌شده

- **Runtime Host** در `src/app/`: `ConfigSource` (خواندن `bootstrap.json`)، `EnvSource` (مرورگری) و `ModuleLoader` (قرارداد دومرحله‌ای `loadManifest` بدون اجرای کد و سپس `loadEntry`) با فراخوانی `core.start()`.
- **`bootstrap.json`:** معماری `0.11`، آداپتر `router` با `BrowserHistoryAdapter`، افزونه‌های `pey.router` و `parsinegar.app`،
  پیکربندی فارسی UI (`fa`/`rtl`).
- **افزونهٔ `parsinegar.app`** در `src/plugins/app/`: مالک مسیرهای `/` و `/not-found` و ثبت آن‌ها در `pey.router.service` هنگام `activate`.
- **رابط کاربری** روی `pey.webui` در `src/ui/`: پوستهٔ برنامه، صفحهٔ خانه با ویرایشگر Markdown راست‌به‌چپ (CodeMirror 6 با حالت markdown)، صفحهٔ not-found با بازگشت از طریق `RouterService`، کاتالوگ فارسی و مونت صریح مسیر اولیه در نقطهٔ ورود.
- **فونت وزیرمتن** به‌صورت محلی در `public/fonts/` به‌همراه سند نمونهٔ فارسی پیش‌فرض.
- **`index.html`** با importmap کامل (بدون ابزار ساخت؛ فقط ESM بومی).
- **۲۳ آزمون** با `node:test` و `jsdom` در `tests/`.
- **مستندات** در `docs/`: `architecture.md` و `decisions.md`.

### وابستگی‌ها

- `@pey/core` از `peyjs/pey-core` (تگ `v0.2.2`)
- `pey.router` از `peyjs/pey-router` (تگ `v1.0.0`)
- `pey.webui` از `peyjs/pey-webui` (تگ `v1.0.0`)
- CodeMirror 6 و `@codemirror/lang-markdown` از npm (بستار import در
  importmap نگاشت شده بود)
