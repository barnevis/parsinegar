# تاریخچهٔ تغییرات پارسی‌نگار

تمام تغییرات قابل‌توجه این پروژه در این فایل ثبت می‌شود.

## [0.7.0] - 1405-07-03

### افزوده‌شده
- خوانایی جدول مارک‌داون در ادیتور: سرستون پررنگ، سطر جداکننده کم‌رنگ و سطرهای بدنه یکی‌درمیان (سورس دست‌نخورده و قابل ویرایش درجا می‌ماند).
- دنبال‌کردن پیوندها در ادیتور: `Ctrl`+کلیک (یا `Cmd` در مک، یا کلیک میانی) روی متن پیوند، مقصد راه‌دور را در تب تازه باز می‌کند؛ کلیک ساده همچنان مکان‌نما را می‌گذارد.
- چینش فایل‌ها از منوی دکمه چینش (آیکون Sort) در نوار فهرست: نام پرونده (الفبایی/عکس الفبایی)، زمان ویرایش و زمان ساخت (نو به کهنه/کهنه به نو)؛ دکمه سند تازه هم آیکون Add Notes گرفت.
- وارد کردن مارک‌داون: از منوی پرونده یا دکمه هدر فایل‌ها (آیکون Upload) فایل `.md` را انتخاب کن تا با نام خودش سند تازه‌ای بسازد و باز کند.
- هایلایت سینتکس بلوک‌های کد (۱۴ زبان: جاوااسکریپت/تایپ‌اسکریپت، پایتون، بش، SQL، JSON/HTML/XML/CSS/YAML، جاوا، C++ گو، راست، PHP) با دکمه رونوشت بالای هر بلاک.
- منوی پرونده: حذف نام برند (نشان در ریل و صفحه درباره است) و افزودن «درباره پارسی‌نگار» با نمایش در ستون وسط.
- صفحه درباره (معرفی، نسخه، پیوند گیت‌هاب و دکمه بازگشت) در ستون وسط به‌جای ادیتور، با ورودی از منوی پرونده؛ ادیتور زیرش mounted می‌ماند تا undo از دست نرود.
- نشان پروژه: دکمه لوگو زیر آیکون تنظیمات در ریل (بازکننده درباره)، hero لوگو در صفحه درباره، پیوند گیت‌هاب در صفحه درباره، favicon و ارجاع README.
- آیکون‌های گوگل در کل پروژه (ریل، هدر فایل‌ها، مودال‌ها): جایگزینی ست stroke قبلی با Material Symbols هم‌خانواده.
- ویرایش هوشمند: ادامه خودکار بازبینه/فهرست/نقل‌قول با Enter (خروج با Enter دوم روی آیتم خالی)، تورفتگی Tab و Shift+Tab، جابه‌جایی خط با Alt+Arrow، بستن خودکار جفت‌کاراکترها (شامل «» و بک‌تیک).
- جست‌وجو و جایگزینی در همین سند: دکمه ذره‌بین ته منوبار (سمت چپ) دراپ‌دان فرم را باز می‌کند (عبارت، جایگزینی، حساسیت به حروف، کلمه کامل، عبارت باقاعده، فقط در انتخاب) با شمارنده «N از M»، قدم‌زدن قبلی/بعدی با Enter، جایگزینی تکی/همه و هایلایت کهربایی مچ‌ها در هر سه تم؛ میان‌برها `Ctrl+F` (فوکس عبارت) و `Ctrl+Shift+F` (فوکس جایگزینی)، مستقل از چیدمان صفحه‌کلید.
- رندر تصویر مارک‌داون با سورس راه‌دور (`http(s)`): `![alt](src)` به‌صورت تصویر واقعی در ادیتور دیده می‌شود (ایستادن روی آن سورس را برای ویرایش برمی‌گرداند)؛ تصویر لینک‌دار (مثل بج‌های README) هم تصویر را نشان می‌دهد و کلیک، لینک را در تب تازه باز می‌کند؛ تصویر خراب همان alt-text می‌شود؛ مسیر نسبی و `data:` همچنان alt-text می‌مانند.
- حالت فقط-خواندن: قفل هر سند از منوی پرونده (ذخیره‌شونده در `parsinegar.documents` نسخه ۰.۳.۰، با نشان «فقط خواندنی» در نوار وضعیت)؛ در قفل، تایپ/نشان‌ها/تسک/جایگزینی بسته و انتخاب/کپی/جست‌وجو/قدم‌زدن باز است، بدون ریمونت و با حفظ undo.
- اسناد داخلی پروژه به‌صورت سند فقط-خواندن در ادیتور: راهنما (`docs/userGuide.md`)، تاریخچه تغییرات (CHANGELOG) و درباره (`docs/ABOUT.md` تازه) از منوی «راهنما» یا نشان ریل (بازگشت هم از همان منو)؛ پنل استاتیک درباره حذف شد.
- تاگل حالت خواندن/نوشتن کنار ذره‌بین منوبار (فقط یکی دیده می‌شود) + جفت «حالت خواندن»/«حالت نوشتن» در منوی نمایش (حالت فعال همیشه غیرقابل انتخاب است).
- تاگل حالت خواندن/نوشتن کنار ذره‌بین منوبار (فقط یکی دیده می‌شود) + جفت «حالت خواندن»/«حالت نوشتن» در منوی نمایش (فعال همیشه غیرقابل انتخاب است).

### تغییر کرده
- کوچک‌سازی صفحه خانه با استخراج هم‌نشین‌ها بدون تغییر رفتار: کنترلر اسناد (working set، autosave، مودال‌ها)، اعمال‌کننده تنظیمات (ترجیحات، زنجیره نوشتن، واچر scheme)، scrollspy فهرست، هلپرهای قالب‌بندی — و مودال‌ها به فرزند واقعی `parsi-modal-dialog` تبدیل شدند.


### رفع‌شده
- چپ‌افتادن بازبینه انجام‌شده (`- [x]`) در حالت خودکار جهت: حرف لاتین `x` داخل چک‌باکس دیگر جهت خط را تعیین نمی‌کند و `- [x] سه` راست‌چین می‌ماند (چک‌باکس خالی تنها هم مبنا می‌گیرد).
- حذف از منوی هر فایل، همان فایل را هدف می‌گیرد نه سند باز: مودال نام همان فایل را نشان می‌دهد و پس از حذف، سند باز (فوکوس، undo و autosave) دست‌نخورده می‌ماند.
- چسبیدن عنوان‌ها و نقل‌قول‌ها به حاشیه مثل متن ساده: فاصله جداکننده بعد از نشان (`# `، `> `) همراه خود نشان پنهان/نمایان می‌شود و دیگر در اندازه قلم عنوان رندر نمی‌شود.
- اعمال واقعی جهت متن صریح: `rtl`/`ltr` همه خطوط را قفل می‌کند (انگلیسی هم از سمت منتخب شروع می‌شود) و فقط بلوک کد چپ‌چین می‌ماند؛ `auto` همچنان هر خط را جدا تشخیص می‌دهد.
- کرش افزونه نمای زنده هنگام کلیک/سلکشن روی سندی با ترکیب نشان‌ها (`Ranges must be added sorted`): دکوریشن‌ها پیش از ساخت مرتب و یکتا می‌شوند.

## [0.6.0] - 1405-06-27

### افزوده‌شده
- منوی هر پرونده در فهرست فایل‌ها: تغییر نام (درون‌خطی با خطای نام تکراری)، دانلود مارک‌داون، مودال ویژگی‌ها (نام، ایجاد، آخرین ویرایش، حجم) و حذف (دکمه حذف از پنل برداشته شد).
- `renameDocument` و `createdAt` در افزونه اسناد (نسخه ۰.۲.۰): یکتایی عنوان با خطای ساخت‌یافته، پسوند فارسی خودکار در ساخت، حفظ تاریخ ایجاد.
- نمایش کلید میان‌بر هر نشان در منوی «افزودن».
- نمایش نشان‌های مارک‌داون فقط در جای مکان‌نما (بقیه نشان‌های همان خط پنهان می‌مانند).
- نمایش نشان‌ها هنگام ایستادن روی متن قالب‌بندی‌شده و بولت/عدد تا لحظه رسیدن مکان‌نما به خود نشان.
- نمایش نشانه‌های عنوان (`#` و `===`) با ایستادن در هر جای خط عنوان، مثل نشانه‌های خطی.

## تغییر کرده
- مبنای راست‌به‌چپ برای حالت خودکار جهت متن تا مکان‌نما روی خط خالی همیشه راست بماند (خطوط دارای نویسه قوی همچنان خودکار تشخیص داده می‌شوند).
- پین جهت خطوط بدون حرف (خالی، فقط‌رقم، نشان تنها) به مبنای سند، چون کرومیوم آن‌ها را در `plaintext` چپ‌چین می‌کند.
- پین صریح جهت و تراز همه خطوط ادیتور از روی اولین حرف قوی (حذف `unicode-bidi: plaintext`): در فایرفاکس سطرهای ادامه که شکست داخل span می‌افتاد چپ‌چین می‌شدند؛ حالا فارسی راست‌چین و انگلیسی چپ‌چین در هر دو موتور.
- رنگ سلکشن روی کد درون‌خطی و بلاک کد: با انتخاب متن، پس‌زمینه کد هم رنگ سلکشن می‌شود (روشن: رنگ سیستمی، تیره/سپیا: همان رنگ متن انتخاب‌شده)؛ پیش‌تر ثابت می‌ماند و حس عدم انتخاب می‌داد.
- هایلایت کم‌رنگ ردیف سند جاری شامل آیکون منو؛ حذف پس‌زمینه خط جاری ویرایشگر.
- حذف پس‌زمینه هاور نام پرونده و دکمه منو در پنل کناری.
- فهرست مطالب: هایلایت کم‌رنگ عنوان فعال مثل سند جاری، قلم ۱۳ پیکسل و دکمه جمع/بازشو زیرعنوان‌ها.
- جداسازی CSS فرزندان میزکار به فایل‌های هم‌مکان (`menu-bar.css` و…) با سازوکار `stylesheetHref` کیت؛ یک قرارداد استایل در کل پروژه.


## [0.5.0] - 1405-06-26

### افزوده‌شده
- بخش تنظیمات: آیکون چرخ‌دنده در انتهای ریل با نمای تنظیمات در پنل کناری — پوسته (روشن/تیره/دستگاه/سپیا با پالت کامل)، جهت متن سند (خودکار/راست‌به‌چپ/چپ‌به‌راست) و استپر اندازه قلم ویرایشگر (۱۲ تا ۲۴ پیکسل).
- افزونهٔ `parsinegar.settings`: ترجیحات اعتبارشده با پیش‌فرض روی `pey.storage` (کالکشن `settings`)، رویداد `settings:changed` و گزارش خطای بحرانی با از دست رفتن استورج.
- پل تم در Entry: استور shared-state کیت + بازتاب روی `documentElement` تا استایل‌های سطح سند و ادیتور هم ببینند؛ `public/theme.css` با اسکوپ‌های تیره و مدیاکوئری سیستم.
- زنجیره‌سازی نوشتن‌های پشت‌سرهم تنظیمات در home و Entry (همگرایی ترتیبی بدون race).
- مهاجرت اسکیمای استورج به نسخه ۲ (ساخت کالکشن `settings` روی پروفایل‌های قدیمی با حفظ سندها).
- پین شدن آیکون تنظیمات به انتهای ریل فعالیت (`align: 'end'` در رجیستری نماها).
- آمار حرف (فقط حروف یونیکد) و حجم پرونده (بایت/کیلوبایت) در نوار وضعیت.
- اسکرول‌اسپای فهرست مطالب: برجسته شدن عنوان فعال هنگام پیمایش ویرایشگر.
- منوی «افزودن» با ۹ نشان مارک‌داون (عنوان، پررنگ، مورب، خط‌خورده، نقل‌قول، پیوند، کد، فهرست مرتب/نامرتب) که در متن درج می‌شوند.
- قلم ۱۲ پیکسل نوار منو و حذف پس‌زمینه و حاشیه دکمه بازشونده.
- ترتیب «برچسب بعد مقدار» در نوار وضعیت (مثل `نویسه ۴۵۵`).
- لهجه teal پیشین (`#5eead4`) در تم تیره و هایلایت `==` در هر سه پوسته (زرد/لیمویی/کهربایی).
- سطح کروم نوار منو و ریل فعالیت (پس‌زمینه + جداکننده هم‌ارز پنل و نوار وضعیت) در تم‌های تیره و سپیا.
- حذف فاصله بین نوار منو و پنل/ریل (حذف `sticky/top` قدیمی اسلات‌ها که به ارتفاع منوبار گره خورده بود).
- نگاشت `device` به `system` کیت هنگام اعمال تم (بدون آن، صفحه روشن می‌ماند ولی ادیتور در سیستم تیره، متن روشن روی زمینه روشن می‌شد).
- ترکیب فرزند واقعی میزکار: چهار المنت (`parsi-menu-bar`، `parsi-activity-rail`، `parsi-side-panel`، `parsi-status-bar`) که صفحهٔ خانه از طریق هلپر مشترک `src/ui/utils/mount.js` مونت می‌کند؛ ارتباط برگشتی فقط رویداد حبابی DOM است.
- ثبت استثنای ترکیب فرزند در `docs/decisions.md` §۱۱ (هلپر `mount.js` تا وقتی کیت ترکیب خودش را بدهد).

### حذف‌شده
- رندر رشته‌ای ناحیه‌ها (`regions.js` و تستش)؛ استایل و منطق هر ناحیه به المنت خودش رفت.

### تغییرکرده
- چیدمان پوشه‌ها طبق `file-layout` بنیان: هر فرزند میزکار در پوشه خودش (`components/<name>/` با `.js` و `.md` هم‌نام)؛ تست‌ها در آینه `tests/components/<name>/`؛ هلپرهای مشترک در `components/workbench/` ماندند.

## [0.4.0] - 1405-06-21

### افزوده‌شده
- تشخیص خودکار جهت هر خط ادیتور: خط فارسی راست‌چین و خط انگلیسی چپ‌چین می‌شود (`unicode-bidi: plaintext` روی هر خط).
- تأییدیه حذف سند با نمایش نام سند در مدال وسط صفحه، هم در پنل فایل‌ها و هم در منوی پرونده.
- کلیدهای میان‌بر ویرایشگر (پررنگ، کج، خط‌خورده، عنوان، نقل‌قول، پیوند، کد و فهرست‌های مرتب و نامرتب) که با موقعیت فیزیکی کلید کار می‌کنند و به چیدمان فارسی/انگلیسی وابسته نیستند.

## [0.3.0] - 1405-06-20

### افزوده‌شده
- مدیریت چندسند با `pey.storage` (IndexedDB) و افزونهٔ `parsinegar.documents`: فهرست/بازکردن/ساخت/حذف و autosave یک‌ثانیه‌ای.
- میزکار: نوار منوی دراپ‌داون (پرونده/ویرایش/نمایش)، ریل آیکون راست با پنل کناری قابل‌تعویض (فایل‌ها/فهرست مطالب)، کارت ویرایشگر و نوار وضعیت زنده (نویسه/واژه/خط).

### تغییرکرده

- ساختار افزونه‌ها آینهٔ افزونه‌های Pey شد: `docs/reference.md`، `lib/` (برای منطق جدا از سیم‌کشی) و `CHANGELOG.md` برای هر افزونه.
- آزمون‌ها هم‌مکان شدند: افزونه‌ها در `tests/` خودشان، رابط کاربری در `src/ui/tests/` (مثل کیت)، و ریشه فقط برای سطح اپ و یکپارچه‌سازی.

### رفع‌شده

- خطاهای رهاشده در جریان‌های سند (تعویض/ساخت/حذف) با `try/catch` مهار شدند.
- شناسهٔ سند بدون `crypto.randomUUID` (HTTP غیرامن) هم ساخته می‌شود.
- جهت ادیتور از پیکربندی می‌آید؛ اعداد آمار فارسی‌اند؛ سطوح از توکن‌های `--pey-*` پیروی می‌کنند؛ چیدمان در عرض کم تاشو است.
- استایل صفحه به شیت خارجی واقعی (`home.css` + `stylesheetHref` کیت) مهاجرت کرد؛ مونت ادیتور با مشاهده‌گر render انجام می‌شود تا پشت گیت استایل گیر نکند.


## [0.2.0] - 1405-06-19

### افزوده‌شده

- نمای زندهٔ تک‌صفحه‌ای ویرایشگر: پنهان‌سازی نشان‌های Markdown، رندر عنوان‌ها/نقل‌قول/فهرست/لینک/کد، کارت کاغذی صفحه و کلیک-برای-فوکوس.
- قالب‌های بازبینه (`- [ ]`/`- [x]` با چک‌باکس کلیک‌شونده)، خط‌خورده (`~~`) و برجسته (`== ==`) با مبنای GFM.

### تغییرکرده

- بستار import استاتیک CodeMirror (`@codemirror/*` ،`@lezer/*` و وابسته‌ها) از نصب transitive به وابستگی مستقیم `package.json` ارتقا یافت؛ همه در importmap نگاشت شده‌اند.

### رفع‌شده

- انتخاب کل متن با Ctrl/⌘+A در چیدمان فارسی (بر اساس موقعیت فیزیکی کلید، نه `event.key`).


## [0.1.0] — 1405-06-16

### افزوده‌شده

- **Runtime Host** در `src/app/`: `ConfigSource` (خواندن `bootstrap.json`)، `EnvSource` (مرورگری) و `ModuleLoader` (قرارداد دومرحله‌ای `loadManifest` بدون اجرای کد و سپس `loadEntry`) با فراخوانی `core.start()`.
- **`bootstrap.json`:** معماری `0.11`، آداپتر `router` با `BrowserHistoryAdapter`، افزونه‌های `pey.router` و `parsinegar.app`،
  پیکربندی فارسی UI (`fa`/`rtl`).
- **افزونهٔ `parsinegar.app`** در `src/plugins/app/`: مالک مسیرهای `/` و `/not-found` و ثبت آن‌ها در `pey.router.service` هنگام `activate`.
- **رابط کاربری** روی `pey.webui` در `src/ui/`: پوستهٔ برنامه، صفحهٔ خانه با ویرایشگر Markdown راست‌به‌چپ (CodeMirror 6 با حالت markdown)، صفحهٔ not-found با بازگشت از طریق `RouterService`، کاتالوگ فارسی و مونت صریح مسیر اولیه در نقطهٔ ورود.
- **فونت وزیرمتن** به‌صورت محلی در `public/fonts/` به‌همراه سند نمونهٔ فارسی پیش‌فرض.
- **`index.html`** با importmap کامل (بدون ابزار ساخت؛ فقط ESM بومی).
- **۲۳ آزمون** با `node:test` و `jsdom` در `tests/`.
- **مستندات** در `docs/`: `architecture.md` و `decisions.md`.

### وابستگی‌ها

- `@pey/core` از `peyjs/pey-core` (تگ `v0.2.2`)
- `pey.router` از `peyjs/pey-router` (تگ `v1.0.0`)
- `pey.webui` از `peyjs/pey-webui` (تگ `v1.0.0`)
- CodeMirror 6 و `@codemirror/lang-markdown` از npm (بستار import در
  importmap نگاشت شده بود)
